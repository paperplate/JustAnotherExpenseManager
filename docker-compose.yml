services:
  expense-manager:
    build: 
      target: debug
    ports:
      - "5000:5000"
    volumes:
      - expense-data:/app/data
    environment:
      - DATABASE_TYPE=sqlite
      - SQLITE_PATH=/app/data/expenses.db
    restart: unless-stopped

volumes:
  expense-data:
